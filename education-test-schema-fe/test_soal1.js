const arrayToCount = [5, 5, 3, 5, 2, 8, 5, 2];
const i = 5;
const result = arrayToCount.filter(i => i === 5).length;
console.log('total data paling banyak keluar adalah ' + i + ' dengan jumlah ' + result);